/**
 * Integration layer abstraction.
 * Add adapters for external services (email, SMS, calendar, payments) here.
 * Each integration should expose a consistent interface per channel.
 */

// export { emailAdapter } from './email.js';
// export { smsAdapter } from './sms.js';
// export { calendarAdapter } from './calendar.js';

export const integrations = {};
